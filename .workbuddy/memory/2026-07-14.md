# 2026-07-14

## Git 分支清理
- 用户要求：保留 `main` 和 `codex/v2-zero-l2`（本地+远程），其余全清。
- 操作：
  - `git worktree prune` + `git worktree remove --force` 清掉 12 个 `worktree-agent-*` worktree 及目录（`.claude/worktrees/` 现为空）。
  - `git branch -D` 删除 10 个普通分支（design/feat/fix/refactor/*）。
  - `git push origin --delete` 删除 `origin/fix/readme-badges`、`origin/refactor/mneme-2.0`（成功）。
- 终态：本地仅 `main` + `codex/v2-zero-l2`；远程跟踪仅 `origin/main` + `origin/codex/v2-zero-l2`。
- 注：`git fetch --prune origin` 因网络 HTTP2 framing layer 报错中断，但远程跟踪引用已自行更新为干净状态。

## README 主页链接修正
- 用户反馈：README 没引导已上线的项目主页 https://scott1743.github.io/mneme/，反而把 introduction 启动页的 python 预览脚本放主位。
- 改动：`README.md` 第 25-32 行——将主页链接提为主引导，python `http.server` 预览降级为可选本地预览；删掉"或等 GitHub Pages 启用后访问"的过时措辞。
- 提交：`e86673a` docs(readme)，仅 stage README.md，未 push（用户未要求）。`.workbuddy/` 未纳入提交。
